#include <cmath>
#include <iostream>
#include <iomanip>
#include <string>
#include <vector>

using namespace std;

int main(int argc, char const *argv[])
{
   // Does optimal require more than one set of "()"? Does it only need the 
   // "[]" in the correct open and close order?
   return 0;
}